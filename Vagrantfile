Vagrant.configure(2) do |config|
	config.vm.define "upstart" do |upstart|
		upstart.vm.box = "ubuntu/trusty64"
	end

	config.vm.define "sysvinit" do |sysvinit|
		sysvinit.vm.box = "puphpet/debian75-x64"
	end
end
